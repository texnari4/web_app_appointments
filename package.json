{
  "name": "beauty-miniapp-monorepo",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "postinstall": "npm --prefix api install && npm --prefix miniapp install && prisma generate --schema=api/prisma/schema.prisma",
    "prebuild": "prisma format --schema=api/prisma/schema.prisma && prisma validate --schema=api/prisma/schema.prisma",
    "build": "npm --prefix api run build && npm --prefix miniapp run build && rm -rf api/public && mkdir -p api/public && cp -R miniapp/dist/* api/public/",
    "start": "npm --prefix api run start:prod"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  }
}